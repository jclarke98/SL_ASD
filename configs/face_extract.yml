# Face embedding extractor parameters
face_extract:
  batch_size: 2048
  data_path: # path to bounding box images generated by preprocess_annot.py 
  manifest_file: # path to annotation files generated by preprocess_annot.py
  save_path: # where to save embeddings 
  n_dataloader_thread: 12
  model_settings:
    pretrained: "vggface2"
    classify: true